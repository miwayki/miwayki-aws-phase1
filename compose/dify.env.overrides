# Añadir al final de vendor/dify/docker/.env (después de copiar .env.example)
# Puertos publicados en el host (evita ocupar 80/443 si ya hay otro servicio)
EXPOSE_NGINX_PORT=9080
EXPOSE_NGINX_SSL_PORT=9443
